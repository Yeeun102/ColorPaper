<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:background="#FAFAFA"
    tools:context=".ui.flashcard.FlashcardCreateFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <!-- 상단 헤더 영역 -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">



            <EditText
                android:id="@+id/etSetTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_toStartOf="@id/btnSave"
                android:layout_centerVertical="true"
                android:layout_marginStart="8dp"
                android:hint="@string/set1"
                android:textStyle="bold"
                android:textSize="25sp"
                android:textColor="#867070"
                android:background="@null"
                android:inputType="text"
                android:importantForAutofill="no" />

            <Button
                android:id="@+id/btnSave"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:backgroundTint="#867070"
                android:text="@string/save"
                android:textSize="12sp" />
        </RelativeLayout>

        <!-- 공개범위 설정 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginTop="20dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/f_visibility"
                android:textSize="14sp"
                android:textColor="#333333"
                android:layout_marginEnd="12dp"/>

            <com.google.android.material.chip.ChipGroup
                android:id="@+id/chipGroupVisibility"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:singleSelection="true"
                app:checkedChip="@+id/chipPublic">

                <com.google.android.material.chip.Chip
                    android:id="@+id/chipPublic"
                    style="@style/Widget.MaterialComponents.Chip.Choice"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/flashcard_private" />

                <com.google.android.material.chip.Chip
                    android:id="@+id/chipFollower"
                    style="@style/Widget.MaterialComponents.Chip.Choice"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/flashcard_onlyF" />

                <com.google.android.material.chip.Chip
                    android:id="@+id/chipPrivate"
                    style="@style/Widget.MaterialComponents.Chip.Choice"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/flashcard_public" />
            </com.google.android.material.chip.ChipGroup>
        </LinearLayout>

        <!-- 단어 카드들이 동적으로 추가될 컨테이너 -->
        <LinearLayout
            android:id="@+id/layoutCardContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginTop="16dp" />

        <!-- 수동 추가 버튼 (+) -->
        <ImageButton
            android:id="@+id/btnNextCard"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="16dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_input_add"
            android:contentDescription="@string/add_card" />

        <!-- 파일 불러오기 영역 -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_toStartOf="@id/btnImportCsv"
                android:text="@string/csv_or_txt"
                android:textSize="15sp"
                android:textColor="#869090"
                android:layout_centerInParent="true"
                android:layout_marginEnd="10dp"/>

            <Button
                android:id="@+id/btnImportCsv"
                android:layout_width="wrap_content"
                android:layout_height="40dp"
                android:layout_alignParentEnd="true"
                android:backgroundTint="#867070"
                android:text="@string/f_upload"
                android:textSize="12sp" />
        </RelativeLayout>

    </LinearLayout>
</ScrollView>